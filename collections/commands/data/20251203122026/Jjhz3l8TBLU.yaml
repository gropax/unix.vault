command: rg "abc" ./dir/ --vimgrep | vi -q -
description: Search for "abc" in "./dir/" with ripgrep in vimgrep format and open
  matches in "vi" quickfix list.
intention: Search for "abc" in "./dir/" and open the matches in an editor quickfix
  list.
tool:
- rg
guid: Jjhz3l8TBLU
aliases:
- rg "abc" ./dir/ --vimgrep | vi -q -
requireSudo: false
