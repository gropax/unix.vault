command: sudo setcap cap_net_raw=ep $(which ping)
description: Grant "ping" the "cap_net_raw" capability so it can use raw sockets without
  full root privileges.
intention: Assign the cap_net_raw capability to the currently used "ping" executable.
tool:
- setcap
fish: false
nixos: TODO
guid: XFQI1SfOtX0
aliases:
- sudo setcap cap_net_raw=ep $(which ping)
requireSudo: true
