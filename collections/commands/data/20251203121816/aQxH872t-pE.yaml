command: rg "abc" ./dir/ --vimgrep | vi -q -
description: Search recursively for "abc" in "./dir/" in vimgrep format and open the
  matches in "vi" quickfix mode.
intention: Search for the string "abc" in "./dir/" and open the search results in
  "vi" quickfix.
tool:
- rg
fish: false
guid: aQxH872t-pE
aliases:
- rg "abc" ./dir/ --vimgrep | vi -q -
requireSudo: false
