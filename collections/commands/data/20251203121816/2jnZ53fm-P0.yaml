command: sudo nixos-rebuild switch --flake .#desktop
description: Build and activate the "desktop" NixOS configuration from the flake in
  the current directory.
intention: Build and switch to the "desktop" NixOS system configuration defined by
  the flake in the current directory.
tool:
- nixos-rebuild
fish: false
nixos: TODO
guid: 2jnZ53fm-P0
aliases:
- sudo nixos-rebuild switch --flake .#desktop
requireSudo: true
