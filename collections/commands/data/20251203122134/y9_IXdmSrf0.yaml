command: sudo nix-collect-garbage -d
description: Delete old and unreachable Nix store paths and profiles, including their
  generations, to free disk space.
intention: Collect and delete old, unreachable Nix store paths and profile generations.
tool:
- nix-collect-garbage
fish: false
guid: y9_IXdmSrf0
aliases:
- sudo nix-collect-garbage -d
nixos: ''
requireSudo: true
