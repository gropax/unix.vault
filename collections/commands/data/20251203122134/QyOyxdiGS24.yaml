command: sudo nixos-rebuild switch --flake .#desktop
description: Build and switch the running NixOS system to the configuration "desktop"
  from the flake in the current directory.
intention: Rebuild and switch the running NixOS system to the ".#desktop" flake configuration.
tool:
- nixos-rebuild
fish: false
guid: QyOyxdiGS24
aliases:
- sudo nixos-rebuild switch --flake .#desktop
nixos: ''
requireSudo: true
